{
  "name": "test",
  "version": "1.0.0",
  "description": "# 1",
  "main": "index.js",
  "scripts": {
    "build": "cd backend && npm install  && cd ../frontend && npm install && npm run build",
    "start": "node backend/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "npm run build"
    
  },
  "repository": {
    "type": "git",
    "url": "t"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/razvanonweb/facultate"
  },
  "homepage": "",
  "dependencies": {
    "dotenv": "^16.x"
  }
}
